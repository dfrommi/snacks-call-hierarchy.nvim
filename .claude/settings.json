{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; echo \"$f\" | grep -q '\\.lua$' && ~/.local/share/nvim/mason/bin/stylua \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Formatting with stylua..."
          }
        ]
      }
    ]
  }
}
